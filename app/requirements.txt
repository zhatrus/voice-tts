# Web API
fastapi==0.115.6
uvicorn==0.34.0
python-multipart==0.0.18

# StyleTTS2 Ukrainian engine — versions follow the upstream HF Space
# (patriotyk/styletts2-ukrainian). torch/torchaudio are installed against a
# CUDA index via PIP_EXTRA_INDEX_URL in the Dockerfile.
torch==2.8.0
torchaudio==2.8.0
transformers==5.0.0
numpy<2
accelerate>=0.26.0

# Audio + model I/O
soundfile
librosa
scipy
einops
einops_exts
munch
pyyaml
huggingface_hub

# Ukrainian text front-end (stress + IPA) and the inference wrapper
git+https://github.com/patriotyk/ukrainian-word-stress.git
git+https://github.com/patriotyk/ipa-uk.git
git+https://github.com/patriotyk/styletts2-inference@209352700cf703a05ff94501290095dd9072fd5f

# Number/acronym verbalization (CTranslate2 m2m100). Optional at runtime,
# controlled by VERBALIZE; required only if VERBALIZE=true.
ctranslate2
sentencepiece
